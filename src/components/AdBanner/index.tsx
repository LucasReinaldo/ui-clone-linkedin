import React from 'react';

import { Container } from './styles';

const AdBanner: React.FC = () => {
  return (
    <Container className="ad-banner">
      <span>LinkedIn - </span>
      Ui clone, looking forward for the next level.
    </Container>
  );
};

export default AdBanner;
